{
  "name": "ballast-landing",
  "private": true,
  "type": "module",
  "description": "Ballast landing page. Separate surface from dashboard/, which stays dependency-free and file://-openable.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "gsap": "^3.13.0",
    "lenis": "^1.3.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
